# 单元测试报告

## 测试框架

- 项目技术栈：Node.js 原生 HTTP + ES Modules。
- 单元测试框架：Vitest。
- 测试重点：Service 层和核心业务逻辑，不测试页面是否能打开。
- 智能药师测试策略：默认使用 Mock；OpenAI 兼容模型调用使用 Stub 模拟 `fetch` 返回，不真实访问大模型 API。

## 覆盖模块与用例

### 药品查询与浏览模块

测试文件：`test/medicineService.test.js`

- 正常情况：按关键词和处方类型查询药品，校验详情页安全提示。
- 异常情况：查询不存在药品详情时抛出 `药品不存在`。
- 边界情况：分页参数小于最小值或超过最大值时自动限制。
- 边界情况：低库存、缺货药品返回正确库存状态和购买动作。

### 购物车与订单模块

测试文件：`test/cartOrderService.test.js`

- 正常情况：OTC 药品加购、订单预览、确认下单、模拟支付并扣减库存。
- 异常情况：缺货药品不可加入购物车。
- 异常情况：未确认购药须知时禁止创建订单。
- 异常情况：处方药订单未审核不得支付。
- 边界情况：重复加购同一药品时数量不能超过库存。
- 边界情况：购物车数量修改必须为正整数且不能超过库存。
- 边界情况：购物车金额满 80 元时计算优惠金额。

### 智能药师咨询模块

测试文件：`test/consultService.test.js`

- 正常情况：Mock 模式下普通咨询不调用真实大模型 API。
- 正常情况：使用 Stub 模拟 OpenAI 兼容大模型返回，校验模型回复来源。
- 异常情况：空咨询内容和不存在的会话抛出业务异常。
- 边界情况：命中多个高风险词时要求转人工并写入风险日志。
- 边界情况：用户主动转人工时更新会话状态。
- 边界情况：模型配置不完整时显示 Mock 本地模拟。

## 运行方式

安装依赖：

```bash
npm install
```

运行单元测试：

```bash
npm test
```

## 测试结果

本轮测试已运行，全部通过。
